% !TeX root = ../../ZF_bmicha_Ana.tex
\subsection{Hauptsatz der Infinitesimalrechnung}
    Für $a \in \mathbb{R}$
    \mathbox{
        \frac{d}{dx} 
        \left[
            \int_a^x f(t) dt
        \right]
        = f(x)
    }